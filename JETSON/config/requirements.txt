# ============================================================================
# JETSON NANO REQUIREMENTS - YOLOv7 + Custom Tracking (OC-Sort / ByteTrack)
# ============================================================================
# Verified compatible with:
# - Jetson Nano with CUDA 10.2
# - Python 3.6.9
# - TensorRT 8.2.1.8
# - torch 1.10.0 + torchvision 0.11.1
# ============================================================================

# Core Deep Learning & Computer Vision
# ============================================================================
torch==1.10.0                     # PyTorch (NVIDIA Jetson wheel)
torchvision==0.11.1               # Vision models (ResNet50, transforms)
opencv-python==4.5.3.56           # Image I/O and preprocessing
numpy==1.19.4                     # Array operations (max compatible version)
tensorrt==8.2.1.8                 # TensorRT inference engine

# Tracking & Multi-Object Detection
# ============================================================================
filterpy==1.4.5                   # Kalman filtering (SORT tracker)
scipy==1.5.4                      # Linear sum assignment (data association)

# Geometric Operations & Utilities
# ============================================================================
shapely==1.8.0                    # Polygon/lane operations
PyYAML==5.4.1                     # Config file parsing
tqdm==4.62.3                      # Progress bars
Pillow==8.4.0                     # Image operations (PIL)
pytz==2025.2                      # Timezone handling (result logging)
psutil==5.8.0                     # System and process resource monitoring

# ============================================================================
# OPTIONAL PACKAGES (Install only if needed for advanced features)
# ============================================================================
# scikit-learn==0.24.2    # For anomaly detection (utils.py)
# moviepy==2.2.1          # For background extraction (setup phase only)
